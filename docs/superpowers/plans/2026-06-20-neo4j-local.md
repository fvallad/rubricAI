# Neo4j Local (Docker) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reemplazar Neo4j Aura Cloud por Neo4j Community Edition en Docker en chimuelo, migrando todos los datos existentes.

**Architecture:** Se agrega un servicio `neo4j` a `docker-compose.python.yml`. Un script Python de migración exporta todos los nodos y relaciones de Aura usando `elementId()` como clave temporal y los recrea en el Neo4j local. Al finalizar la migración, se actualizan las variables de entorno y se reinician los servicios.

**Tech Stack:** Neo4j 5 Community (Docker), Python `neo4j` driver (ya en requirements), Docker Compose.

## Global Constraints

- Neo4j edición: Community (gratuita) — single database, solo `neo4j` por defecto
- El código en `rubricai_ai/neo4j_client.py` ya maneja el fallback a single database en `try_create_database()`
- No modificar `neo4j_client.py` — funciona sin cambios con la nueva config
- La migración se corre UNA sola vez, desde chimuelo, antes de cambiar el `.env`
- No hacer `git commit` del archivo `.env` — está en `.gitignore`

---

## Mapa de archivos

| Archivo | Acción | Responsabilidad |
|---|---|---|
| `docker-compose.python.yml` | Modificar | Agregar servicio `neo4j`, volumen `neo4j_data`, `depends_on` en `python_rag` |
| `.env.example` | Modificar | Actualizar variables NEO4J_* a config local; eliminar vars de Aura |
| `.env` | Modificar (NO commitear) | Actualizar NEO4J_* a local DESPUÉS de migración |
| `rubricai_ai/migrate_neo4j.py` | Crear | Script one-shot de migración Aura → local |

---

## Task 1: Agregar servicio Neo4j a docker-compose.python.yml

**Files:**
- Modify: `docker-compose.python.yml`

**Interfaces:**
- Produces: servicio `neo4j` en la red Docker, accesible como `neo4j:7687` desde otros containers; volumen `neo4j_data` para persistencia

- [ ] **Step 1: Reemplazar el contenido de `docker-compose.python.yml`**

El archivo completo queda así:

```yaml
name: rubricai_ai

services:
  neo4j:
    image: neo4j:5-community
    container_name: rubricai_neo4j
    restart: unless-stopped
    ports:
      - "7474:7474"
      - "7687:7687"
    environment:
      - NEO4J_AUTH=${NEO4J_AUTH}
    volumes:
      - neo4j_data:/data
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- localhost:7474 > /dev/null 2>&1"]
      interval: 10s
      timeout: 10s
      retries: 10
      start_period: 40s

  python_rag:
    build: ./rubricai_ai
    container_name: python_rag
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ./rubricai_ai:/app
      - hf_cache:/root/.cache/huggingface
    depends_on:
      neo4j:
        condition: service_healthy

  frontend:
    build: ./frontend
    container_name: astro_frontend
    restart: unless-stopped
    ports:
      - "4321:4321"
    env_file: .env
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - python_rag

volumes:
  hf_cache:
  neo4j_data:
```

- [ ] **Step 2: Agregar `NEO4J_AUTH` al `.env` (solo para arrancar el container — todavía apuntamos a Aura)**

Agregar esta línea al `.env` existente (elegí una contraseña segura en lugar de `changeme`):

```
NEO4J_AUTH=neo4j/changeme
```

> Nota: Esta variable la usa el *container de Neo4j* para setear su propia contraseña al arrancar por primera vez. El valor después de `/` debe coincidir con el `NEO4J_PASSWORD` que vas a poner en el Step 3 de Task 3.

- [ ] **Step 3: Levantar solo el container de Neo4j para verificar que arranca**

```bash
cd /home/fer/proyectos/rubricai
docker compose -f docker-compose.python.yml up neo4j -d
```

Esperar ~30 segundos y verificar que el healthcheck pasa:

```bash
docker compose -f docker-compose.python.yml ps neo4j
```

Salida esperada: `rubricai_neo4j   Up (healthy)`

- [ ] **Step 4: Verificar acceso al Neo4j Browser**

Abrir en el navegador: `http://chimuelo:7474`  
Loguearse con usuario `neo4j` y la contraseña elegida.  
Ejecutar en la consola: `MATCH (n) RETURN count(n)` — debe devolver `0` (base vacía).

- [ ] **Step 5: Commit**

```bash
git add docker-compose.python.yml
git commit -m "feat: agregar servicio Neo4j Community local a docker-compose"
```

---

## Task 2: Crear script de migración

**Files:**
- Create: `rubricai_ai/migrate_neo4j.py`

**Interfaces:**
- Consumes: credenciales Aura y local por args de línea de comando
- Produces: todos los nodos y relaciones de Aura copiados al Neo4j local

- [ ] **Step 1: Crear `rubricai_ai/migrate_neo4j.py`**

```python
#!/usr/bin/env python3
"""One-time migration script: Neo4j Aura Cloud -> Neo4j local."""
import argparse
import logging
from neo4j import GraphDatabase

logging.basicConfig(level=logging.INFO, format="%(asctime)s %(levelname)s %(message)s")
log = logging.getLogger(__name__)


def export_nodes(session):
    result = session.run(
        "MATCH (n) RETURN elementId(n) AS eid, labels(n) AS labels, properties(n) AS props"
    )
    return [{"eid": r["eid"], "labels": r["labels"], "props": dict(r["props"])} for r in result]


def export_rels(session):
    result = session.run(
        """
        MATCH (a)-[r]->(b)
        RETURN elementId(a) AS src, elementId(b) AS tgt,
               type(r) AS rel_type, properties(r) AS props
        """
    )
    return [
        {"src": r["src"], "tgt": r["tgt"], "rel_type": r["rel_type"], "props": dict(r["props"])}
        for r in result
    ]


def import_nodes(session, nodes):
    for node in nodes:
        labels_str = ":".join(node["labels"])
        props = {**node["props"], "_mig_id": node["eid"]}
        session.run(f"CREATE (n:{labels_str} $props)", props=props)
    log.info("Imported %d nodes", len(nodes))


def import_rels(session, rels):
    for rel in rels:
        cypher = (
            f"MATCH (a {{_mig_id: $src}}), (b {{_mig_id: $tgt}}) "
            f"CREATE (a)-[r:{rel['rel_type']} $props]->(b)"
        )
        session.run(cypher, src=rel["src"], tgt=rel["tgt"], props=rel["props"])
    log.info("Imported %d relationships", len(rels))


def cleanup_mig_id(session):
    session.run("MATCH (n) WHERE n._mig_id IS NOT NULL REMOVE n._mig_id")
    log.info("Cleaned up _mig_id temporary attributes")


def main():
    parser = argparse.ArgumentParser(description="Migrate Neo4j Aura -> local")
    parser.add_argument("--source", required=True, help="Aura URI (neo4j+s://...)")
    parser.add_argument("--source-user", required=True, help="Aura username")
    parser.add_argument("--source-pass", required=True, help="Aura password")
    parser.add_argument("--target", required=True, help="Local bolt URI (bolt://localhost:7687)")
    parser.add_argument("--target-user", required=True, help="Local Neo4j username")
    parser.add_argument("--target-pass", required=True, help="Local Neo4j password")
    args = parser.parse_args()

    log.info("Connecting to source (Aura)...")
    src_driver = GraphDatabase.driver(args.source, auth=(args.source_user, args.source_pass))

    log.info("Connecting to target (local)...")
    tgt_driver = GraphDatabase.driver(args.target, auth=(args.target_user, args.target_pass))

    try:
        src_driver.verify_connectivity()
        log.info("Source connection OK")
        tgt_driver.verify_connectivity()
        log.info("Target connection OK")

        log.info("Exporting nodes from Aura...")
        with src_driver.session() as session:
            nodes = export_nodes(session)
        log.info("  -> %d nodes exported", len(nodes))

        log.info("Exporting relationships from Aura...")
        with src_driver.session() as session:
            rels = export_rels(session)
        log.info("  -> %d relationships exported", len(rels))

        log.info("Importing nodes to local...")
        with tgt_driver.session() as session:
            import_nodes(session, nodes)

        log.info("Importing relationships to local...")
        with tgt_driver.session() as session:
            import_rels(session, rels)

        log.info("Cleaning up temporary migration attributes...")
        with tgt_driver.session() as session:
            cleanup_mig_id(session)

        log.info("Migration complete!")
        log.info("Summary: %d nodes, %d relationships migrated", len(nodes), len(rels))

    finally:
        src_driver.close()
        tgt_driver.close()


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Verificar que el script arranca sin errores de sintaxis**

```bash
cd /home/fer/proyectos/rubricai
python rubricai_ai/migrate_neo4j.py --help
```

Salida esperada: el help de argparse con los 6 argumentos.

- [ ] **Step 3: Ejecutar la migración**

Reemplazá `AURA_PASS` con la contraseña real de Aura (la del `.env` actual) y `LOCAL_PASS` con la contraseña que elegiste en Task 1 Step 2:

```bash
python rubricai_ai/migrate_neo4j.py \
  --source "neo4j+s://aa211074.databases.neo4j.io" \
  --source-user "aa211074" \
  --source-pass "AURA_PASS" \
  --target "bolt://localhost:7687" \
  --target-user "neo4j" \
  --target-pass "LOCAL_PASS"
```

Salida esperada (ejemplo):
```
2026-06-20 ... INFO Source connection OK
2026-06-20 ... INFO Target connection OK
2026-06-20 ... INFO Exporting nodes from Aura...
2026-06-20 ... INFO   -> 42 nodes exported
2026-06-20 ... INFO Exporting relationships from Aura...
2026-06-20 ... INFO   -> 38 relationships exported
2026-06-20 ... INFO Importing nodes to local...
2026-06-20 ... INFO Imported 42 nodes
2026-06-20 ... INFO Importing relationships to local...
2026-06-20 ... INFO Imported 38 relationships
2026-06-20 ... INFO Cleaning up temporary migration attributes...
2026-06-20 ... INFO Cleaned up _mig_id temporary attributes
2026-06-20 ... INFO Migration complete!
```

- [ ] **Step 4: Verificar datos en Neo4j Browser**

En `http://chimuelo:7474`, ejecutar:

```cypher
MATCH (n) RETURN labels(n), count(n) ORDER BY count(n) DESC
```

Debe mostrar los mismos conteos que en Aura. Verificar también:

```cypher
MATCH (r:Rubric) RETURN r.id, r.title
```

- [ ] **Step 5: Commit**

```bash
git add rubricai_ai/migrate_neo4j.py
git commit -m "feat: script de migración Neo4j Aura -> local"
```

---

## Task 3: Actualizar variables de entorno y reiniciar servicios

**Files:**
- Modify: `.env` (NO commitear)
- Modify: `.env.example`

**Interfaces:**
- Consumes: Neo4j local corriendo y con datos migrados (Task 1 + Task 2)
- Produces: `python_rag` y `frontend` apuntando al Neo4j local

- [ ] **Step 1: Actualizar `.env` para apuntar al Neo4j local**

Reemplazar el bloque Neo4j en `.env`. El `NEO4J_PASSWORD` debe coincidir con la parte después de `/` en `NEO4J_AUTH`:

```env
# Neo4j Database Configuration (local Docker)
NEO4J_URI=bolt://neo4j:7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=changeme
NEO4J_DATABASE=neo4j
NEO4J_AUTH=neo4j/changeme
```

Eliminar estas líneas (ya no aplican):
```
AURA_INSTANCEID=...
AURA_INSTANCENAME=...
```

- [ ] **Step 2: Actualizar `.env.example` con la nueva config local**

Reemplazar el bloque Neo4j en `.env.example`:

```env
# Neo4j Database Configuration (local Docker — levantado por docker-compose.python.yml)
NEO4J_URI=bolt://neo4j:7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=changeme
NEO4J_DATABASE=neo4j
NEO4J_AUTH=neo4j/changeme
```

Eliminar las líneas `AURA_INSTANCEID` y `AURA_INSTANCENAME` del `.env.example`.

- [ ] **Step 3: Reiniciar `python_rag` y `frontend`**

```bash
cd /home/fer/proyectos/rubricai
docker compose -f docker-compose.python.yml up python_rag frontend -d --force-recreate
```

- [ ] **Step 4: Verificar que `python_rag` se conecta al Neo4j local**

```bash
docker logs python_rag --tail 30
```

Buscar la línea:
```
INFO:neo4j_client:Connecting to Neo4j at bolt://neo4j:7687 ...
INFO:neo4j_client:Successfully connected to Neo4j server!
```

- [ ] **Step 5: Verificar endpoint de la API**

```bash
curl http://localhost:8000/rubrics
```

Debe devolver el listado de rúbricas (las mismas que estaban en Aura).

- [ ] **Step 6: Commit de `.env.example`**

```bash
git add .env.example
git commit -m "chore: actualizar .env.example a config Neo4j local"
```
