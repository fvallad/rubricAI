Actividad: Actividad Integradora
Tipo: page

Descripción:


Contenido de la página:



  
  
  CONSTRUCCIÓN DE LA PROPUESTA DE ESTUDIO DE CASO
  
  
  




    
  
    CONSTRUCCIÓN DE LA PROPUESTA DE ESTUDIO DE CASO



  

    En este módulo vas a construir paso a paso una propuesta de estudio de caso alineada con los contenidos, habilidades y estrategias abordadas en el curso. Podrás usar herramientas de IA para diseñar, revisar y enriquecer tu propuesta antes de entregarla. Si bien podés ir guardando tus avances, que tendrás disponibles siempre que ingreses desde el mismo navegador, en el mismo dispositivo, te sugerimos que guardes copias descargadas cada vez que hagas modificaciones.
  

  
    
      Datos personales 
      1. Contenidos
      2. Habilidades
      3. Diseño del caso
      4. Preguntas críticas
      5. Recorrido
      6. Autoevaluación

    
    
      

        
          Nombre del participante
          Ingresá tu nombre y apellido:
          
        

        
          1. Contenidos seleccionados
          Seleccioná los contenidos o temas y analizá su potencialidad para abordarlos a través de un caso. Podés plantear una fundamentación de esta decisión.
          
          Una vez seleccionados te pedimos que especifiques y elabores una lista de los objetivos y los contenidos a abordar. Sumá tantos como necesites.
          
          + Agregar objetivos y contenidos
          ¿En qué secuencia de enseñanza lo incluirías?
          
        

        
          2. Habilidades y experiencias
          Teniendo en cuenta los objetivos y contenidos seleccionados, comenzá a definir 
 las experiencias de aprendizaje que planificás ofrecer a los estudiantes:
          
          Desde allí, anticipá: ¿qué habilidades y procesos cognitivos proponés que pongan en juego con la estrategia de estudio de casos?
          
          + Agregar ítem
          Mapeo de habilidades:
          
        

        
          3. Diseño del caso
          Esta actividad supone la construcción o revisión de un caso teniendo en cuenta los criterios sobre un “buen caso” abordados en el EJE: ENSEÑAR CON IA, y utilizar la ayuda de la IA recuperando el material del EJE: CASOS CON ASISTENCIA ALGORÍTMICA.

 Para ello te invitamos a:

          Construir el núcleo narrativo o relato: planteo de contexto, personajes, situaciones, dilemas presentados o problemas
          
          Organizar la información y diseño de imágenes: Diseñar cómo se presentará la información. Plantear información extra que se va a ofrecer a los estudiantes o que se les pedirá que investiguen.
          
          Definir de perspectivas de análisis: A partir de los contenidos ya establecidos definir los diferentes puntos de análisis.
          
        

        
          4. Preguntas críticas
          Ahora nos centraremos en el diseño de preguntas críticas que promuevan la reflexión de los estudiantes. Tener en cuenta lo planteado en el EJE: ENSEÑAR CON CASOS. 

Será clave que estas preguntas ayuden a reconocer dilemas, posturas, problemas y que se promueva el intercambio grupal. Podés hacer uso de la asistencia de la IA para la revisión de estas preguntas teniendo en cuenta las características de las mismas.
          
          + Agregar pregunta
        

        
          5. Recorrido de la propuesta
          Plantear el recorrido de la propuesta en forma completa y en su versión final. Podés hacer uso de la asistencia de la IA para organizar la información. 
Definir:

          Presentación de las consignas:
          
          Etapas de realización:
          
          Agrupamientos y tareas:
          
          Entornos de trabajo:
          
          Evaluación y seguimiento:
          
        

        
          6. Autoevaluación y metacognición
          Definir ¿Cómo se invitará a realizar una autoevaluación y un proceso metacognitivo que recupere el proceso grupal e individual realizado.
          
          Plantear algunas preguntas para promover este tipo de reflexión:
          
          + Agregar pregunta
        


  7. Entregar mi propuesta
  A continuación encontrarás el espacio de entrega de tu actividad. Una vez que tengas listo tu archivo PDF, podés subirlo directamente haciendo clic en "Agregar entrega".

  
  




        
          Guardar progreso
          Descargar PDF
        
      
    
  



  function agregarCampo(contenedorID, clase) {
    const contenedor = document.getElementById(contenedorID);
    const nuevo = document.createElement('textarea');
    nuevo.className = clase;
    nuevo.rows = 3;
    nuevo.style.width = '100%';
    nuevo.style.marginBottom = '0.8em';
    contenedor.appendChild(nuevo);
  }

  function mostrarContenido(id) {
    if (!document.getElementById(id)) return;

    // Oculta todas las secciones
    document.querySelectorAll('.contenido').forEach(seccion => seccion.classList.remove('visible'));

    // Muestra la sección actual
    const activa = document.getElementById(id);
    if (activa) activa.classList.add('visible');

    // Actualiza la clase activa del menú
    const botones = document.querySelectorAll('.item-menu');
    botones.forEach(boton => boton.classList.remove('activo'));

    const index = id.replace('seccion', '');
    const botonActivo = document.querySelector(`#sidebar .item-menu:nth-child(${parseInt(index)+1})`);
    if (botonActivo) botonActivo.classList.add('activo');

    // Actualiza la URL sin recargar
    const nuevaUrl = new URL(window.location);
    nuevaUrl.hash = '#' + id;
    history.replaceState(null, '', nuevaUrl);
  }

  function guardarProgreso() {
    const campos = document.querySelectorAll('textarea, input[type="text"]');
    campos.forEach(campo => {
      const key = campo.id || campo.className;
      if (key) localStorage.setItem(key, campo.value);
    });
    alert("Progreso guardado correctamente.");
  }

  async function generarPropuestaPDF() {
    const { jsPDF } = window.jspdf;
    const doc = new jsPDF();
    const nombre = document.getElementById("nombreParticipante").value.trim() || "sin_nombre";

    doc.setFontSize(14);
    doc.text("Propuesta de Estudio de Caso", 10, 10);
    doc.setFontSize(12);
    doc.text("Participante: " + nombre, 10, 20);
    let y = 30;

    const secciones = document.querySelectorAll('.contenido');
    secciones.forEach((sec, idx) => {
      if (idx === 0) return; // Saltar sección 0 (nombre)

      const titulo = sec.querySelector('h2')?.innerText || "Sección";
      doc.setFontSize(12);
      doc.text(titulo, 10, y);
      y += 6;

      const textos = sec.querySelectorAll('textarea');
      textos.forEach((campo, i) => {
        const texto = campo.value.trim();
        if (texto) {
          const lineas = doc.splitTextToSize(texto, 180);
          doc.text(lineas, 10, y);
          y += lineas.length * 6 + 2;
          if (y > 270) {
            doc.addPage();
            y = 10;
          }
        }
      });
    });

    doc.save("Propuesta_" + nombre.replace(/\s+/g, '_') + ".pdf");
  }

  document.addEventListener("DOMContentLoaded", function () {
    // Restaurar datos del localStorage
    document.querySelectorAll('textarea, input[type="text"]').forEach(campo => {
      const key = campo.id || campo.className;
      if (localStorage.getItem(key)) campo.value = localStorage.getItem(key);
    });

    // Mostrar la sección actual por hash o default
    const hash = window.location.hash || '#seccion0';
    mostrarContenido(hash.replace('#', ''));
  });

  // Cuando cambia el hash (por navegación o clic externo), mostrar la nueva sección
  window.addEventListener('hashchange', function () {
    const nuevaSeccion = window.location.hash.replace('#', '');
    mostrarContenido(nuevaSeccion);
  });